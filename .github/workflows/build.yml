name: "1. Build"

on:
  push:

env:
  if: github.ref == 'main'
  IMAGE_TAG: latest

  if: github.ref == 'env1'
  IMAGE_TAG: env1

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
      - name: Default Environment
        run: env

      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Hello World
        run: |
          ./hello-world.sh
          env
